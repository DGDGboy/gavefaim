<div class="reviewNew" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @restaurant.images[0].key %>);">
  <div class="container">
    <div class="row">
      <div class="col-6 offset-3">
        <h1>Rédiger un commentaire</h1>
        <%= simple_form_for [ @restaurant, @review ] do |f| %>
          <%= f.input :content %>
          <div class="bouton">
            <%= f.button :submit, "Submit review" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>



